.DS_Store
Thumbs.db
*.tmp

# Personal data silo — never committed. See private.example/README.md.
/private/

# Wrangler local cache/build output - never commit
worker/.wrangler/

# npm install output, if anyone runs it locally (worker/package.json is
# mainly for the Deploy to Cloudflare button, which installs its own copy)
node_modules/
